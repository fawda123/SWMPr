# library path
.libPaths('C:\\Users\\mbeck\\R\\library')

# startup message
cat('SWMPr...\n')

# packages to use
# install.packages("SSOAP", repos="http://www.omegahat.org/R", dependencies = T, 
#   type =  "source")
library(SSOAP)
library(XML)
library(ggplot2)
library(plyr)
library(data.table)
library(devtools)

# load SWMPr package
load_all()
library(SWMPr)

# ggplot theme set
theme_set(theme_bw())