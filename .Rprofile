# library path
.libPaths('C:\\Users\\mbeck\\R\\library')

# startup message
cat('SWMPr...\n')

# packages to use
# install.packages("SSOAP", repos="http://www.omegahat.org/R", dependencies = T, 
#   type =  "source")
library(SSOAP)
library(XML)
library(RCurl)
library(ggplot2)
library(plyr)

# functions to use
source('funcs.r')

theme_set(theme_bw())